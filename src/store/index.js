import { createStore } from 'vuex'
import toast from "@/store/toast"

export default createStore({
  state: {
  },

  mutations: {
  },

  actions: {
  },

  getters: {
  },

  modules: {
    toast
  }
})
